box: golang

build:
  steps:
    - script:
      name: echo golang information
      code: go version
    - script:
      name: install packages
      code: make install
    - script:
      name: test
      code: make test
